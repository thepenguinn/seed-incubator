\begin{tikzpicture} [
        transform shape,
        %rounded corners = 0.3cm,
        every path/.style = {
            thick,
            > = latex,
        },
        %shorten > = 8pt,
        %shorten < = 8pt,
    ]

    \draw
    (0,0) coordinate (origin)
    ;

    \subSeedBaseMarked {base} {origin} {center}


\end{tikzpicture}
